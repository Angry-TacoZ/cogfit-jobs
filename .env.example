# Optional. The prototype works without this by using the deterministic mock evaluator.
# Never expose model API keys with VITE_ prefixes.
LLM_API_KEY=
LLM_PROVIDER=openai

# Public app mode is safe to expose. It does not contain secrets.
VITE_COGFIT_EVALUATOR_MODE=mock
